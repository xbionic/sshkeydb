using a configfile (done)
write a client for the database to retrieve keys and add them to the auth-file (done)
more database support like noSQL (mongodb, couchdb) and SQL(mySQL, postgreSQL)
add a function to set a key active/inactive

More database control (stored procedures, replication)

Future Plans:
Write a GUI client to deploy the keys to other networks.
Optimize the Code.
Find the strength of the key and type (ssh-keygen -l -f .ssh/thekey.pub)